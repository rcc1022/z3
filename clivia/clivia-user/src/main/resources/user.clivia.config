## 设置是否启动时同步用户数据。
#clivia.user.sync=false

## 设置最大用户数。
## 小于等于0表示不限制。
#clivia.user.full=0

## 设置在线时间有效时长，单位：分钟。
## 最后一次访问超过此设置时，将被视为下线。
#clivia.user.online.effective=30

## 设置是否启用权限校验。
#clivia.user.crosier.on=false

## 设置推荐注册码有效期，单位：天。
#clivia.user.inviter.expiry=7
