## 设置全局锁类型。
# 0-内部Map，适用于单节点；
# 1-内存表，适用于多节点。
#clivia.lock.type=0

## 设置全局锁使用的数据源key。
#clivia.lock.data-source=

## 设置异步处理最大线程数。
#clivia.async.size=64

## 设置缓存时长，0表示不缓存，单位：分钟。
#clivia.util.carousel.cache-time=5
