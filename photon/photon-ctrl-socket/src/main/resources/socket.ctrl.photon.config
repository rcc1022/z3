## 设置Socket服务端口号。
## 小于等于零表示不启用Socket服务。
#photon.ctrl.socket.port=0

## 设置Socket服务最大处理线程数。
#photon.ctrl.socket.max-thread=64

## 设置Socket返回数据压缩阀值。
## 当返回数据大小超过此设置时，将对数据进行压缩；否则不压缩。
#photon.ctrl.socket.zip-size=4096
